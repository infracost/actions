{
  "name": "infracost-actions",
  "description": "Infracost GitHub Actions",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/infracost/actions.git"
  },
  "author": "Infracost",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/infracost/actions/issues"
  },
  "homepage": "https://github.com/infracost/actions#readme",
  "directories": {
    "scripts": "scripts"
  },
  "scripts": {
    "examples:generate_tests": "node scripts/generateExamplesTests.js",
    "examples:test": "act -W .github/workflows/examples_test.yml -s GITHUB_TOKEN=$GITHUB_TOKEN -s INFRACOST_API_KEY=$(infracost configure get api_key)",
    "examples:update_golden": "act -W .github/workflows/examples_test.yml -s GITHUB_TOKEN=$GITHUB_TOKEN -s INFRACOST_API_KEY=$(infracost configure get api_key) --env UPDATE_GOLDEN_FILES=true -b && rm -rf workflow"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
