name: Scanner Release (Disabled)

on:
  push:
    tags:
      - "scanner/v*.*.*"
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: |
          echo "::error::Scanner releases are published from infracost/ci; cherry-pick hotfixes there before releasing."
          exit 1
